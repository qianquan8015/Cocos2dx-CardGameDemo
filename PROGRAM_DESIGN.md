# 程序设计说明

## 1. 设计目标

本项目目标是实现需求一的最小可运行版本。重点不是完整纸牌关卡系统，而是先跑通纸牌消除 Demo 的核心操作：

- 点击桌面牌后，根据点数差 1 判断是否能匹配当前顶部牌。
- 点击备用牌后，不做匹配判断，直接替换当前顶部牌。
- 有效移动使用 `MoveTo` 动画。
- 每次有效移动记录历史。
- 支持连续撤销回退。
- 动画期间锁定输入，避免状态混乱。

## 2. 架构设计

项目采用视图、逻辑、数据分离：

- 数据层只保存状态，不负责显示。
- 视图层只负责显示、点击和动画，不判断游戏规则。
- 逻辑层负责规则判断、状态更新、历史记录和撤销。
- 场景层负责组织对象和连接 View 与 Controller。

这样做的好处是：后续替换真实图片牌面、增加新布局或扩展规则时，不需要把所有代码都塞进一个 Scene 里。

## 3. 模块说明

### CardGameScene

- 场景入口。
- 创建上方主牌区和下方堆牌区背景。
- 初始化固定测试牌局。
- 创建撤销按钮。
- 创建 `CardView`。
- 将卡牌点击事件转交给 `GameController`。
- 根据历史记录刷新撤销按钮状态。

### CardData

保存单张卡牌的数据：

- 卡牌唯一 `id`。
- 点数 `rank`。
- 花色 `suit`。
- 所在区域 `area`。
- 是否正面 `faceUp`。
- 原始位置 `originalPos`。
- 当前位置 `currentPos`。

### GameState

保存当前游戏状态：

- 全部卡牌数据。
- 当前顶部牌 id。
- 桌面牌 id 列表。
- 备用牌 id 列表。
- 历史记录栈。

### MoveRecord

保存一次可回退操作：

- 移动的卡牌 id。
- 原区域和目标区域。
- 原位置和目标位置。
- 移动前的顶部牌。
- 移动后的顶部牌。
- 移动类型。

### CardRule

只负责规则判断：

- 判断两张牌点数差是否为 1。
- 不判断花色。

### CardView

只负责卡牌表现：

- 显示卡牌。
- 监听点击。
- 执行 `MoveTo` 动画。
- 不判断匹配规则。
- 不直接修改游戏状态。

### GameController

负责游戏操作流程：

- 处理卡牌点击。
- 判断点击的是桌面牌还是备用牌。
- 调用 `CardRule` 判断是否可匹配。
- 生成 `MoveRecord`。
- 更新 `GameState`。
- 执行撤销。
- 动画期间锁定输入。

### GameConfig

集中保存配置：

- 设计分辨率。
- 主牌区和堆牌区尺寸。
- 卡牌尺寸。
- 卡牌坐标。
- 动画时间。
- 背景颜色。

## 4. 核心流程

### 桌面牌点击匹配流程

1. 玩家点击桌面牌。
2. `CardView` 将卡牌 id 回调给 `CardGameScene`。
3. `CardGameScene` 转交给 `GameController`。
4. `GameController` 检查输入是否锁定、卡牌是否正面。
5. 取出当前顶部牌。
6. 调用 `CardRule::canMatch` 判断点数差是否为 1。
7. 如果可以匹配，生成 `MoveRecord`。
8. 移除桌面牌列表中的该卡牌。
9. 锁定输入，播放 `MoveTo` 动画。
10. 动画结束后更新当前顶部牌、写入历史记录、解除输入锁。

### 备用牌点击替换流程

1. 玩家点击备用牌。
2. `GameController` 检查该牌是否为当前可用备用牌。
3. 不做点数匹配判断。
4. 生成 `MoveRecord`。
5. 从备用牌列表移除该牌。
6. 播放移动动画。
7. 动画结束后将该牌设为新的顶部牌，并写入历史记录。

### 撤销回退流程

1. 玩家点击撤销按钮。
2. `GameController` 检查历史记录是否为空。
3. 取出最近一次 `MoveRecord`。
4. 锁定输入。
5. 将对应卡牌移动回 `fromPos`。
6. 动画结束后恢复卡牌区域、当前位置、当前顶部牌。
7. 如果卡牌来自桌面牌或备用牌，则放回对应 id 列表。
8. 解除输入锁，刷新撤销按钮状态。

## 5. 状态恢复设计

每张牌需要唯一 id，因为撤销时不能只依赖牌面文字。两张牌可能点数和花色相同，但它们仍然是不同对象。唯一 id 可以稳定找到具体卡牌数据和对应视图。

撤销不能只做动画。动画只改变画面位置，如果不恢复 `GameState`，后续点击判断会使用错误的顶部牌、错误的区域列表和错误的历史记录。因此撤销必须同时恢复：

- 卡牌区域。
- 卡牌当前位置。
- 当前顶部牌 id。
- 桌面牌或备用牌列表。
- 历史记录。

动画期间需要锁定输入，因为连续点击可能在前一次动画还没结束时再次修改状态，导致顶部牌、历史记录和视图位置不一致。

## 6. 扩展方式

后续可以按当前结构继续扩展：

- 新卡牌：增加 `CardData` 并创建对应 `CardView`。
- 新牌局布局：修改 `CardGameScene::initFixedCards` 或引入关卡配置。
- 新回退类型：扩展 `MoveType` 和 `MoveRecord`。
- 真实图片牌面：只改 `CardView`，使用 `Resources/res` 中的底图、数字和花色资源。
- 遮挡翻牌机制：在 `GameState` 中记录遮挡关系，由 `GameController` 在移动后判断是否翻开下层牌。
